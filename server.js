// Root entry file for Hostinger's Node.js (Express) hosting.
// Hostinger looks for the entry file relative to the repository root and detects the
// framework from the root package.json, so this file simply boots the backend server.
require('./backend/src/server');
